<UserControl x:Class="WhatAreYouDoing.Display.History.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:common="clr-namespace:WhatAreYouDoing.Display.Common"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DatePicker Grid.Row="0" SelectedDate="{Binding SelectedDate}" Width="100" Margin="10" />
        <common:EntriesGrid Grid.Row="1"/>
    </Grid>
</UserControl>